# ========== Database (choose ONE setup below) ==========

# --- Option A: PostgreSQL on YOUR PC (recommended for personal use) ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=tlou_clothing

# --- Option B: Free cloud DB (Neon / Supabase) — paste host from their dashboard ---
# DB_HOST=ep-xxxx.region.aws.neon.tech
# DB_PORT=5432
# DB_USER=your_cloud_user
# DB_PASSWORD=your_cloud_password
# DB_NAME=neondb
# DB_SSL=true

# --- OLD work server — do NOT use for personal project ---
# DB_HOST=10.0.0.65
# DB_USER=xisekelo
# DB_NAME=xisekelo

# ========== Server ==========
PORT=3003
NODE_ENV=development
JWT_SECRET=change-this-to-a-long-random-string

# ========== Email verification (optional — Gmail app password) ==========
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tlou.masoga1@gmail.com
# SMTP_PASS=your-gmail-app-password
# SMTP_FROM=T.L.O.U. Clothing <tlou.masoga1@gmail.com>
